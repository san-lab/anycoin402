<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Your Story</title>
  <style>
    body { font-family: Georgia, serif; margin: 40px; line-height: 1.6; }
    h1 { font-size: 24px; margin-bottom: 20px; }
    .story { margin-top: 30px; }
    .back-button {
      display: inline-block;
      margin-top: 30px;
      padding: 10px 20px;
      font-size: 16px;
      text-decoration: none;
      background-color: #007BFF;
      color: white;
      border-radius: 6px;
      transition: background-color 0.2s ease-in-out;
    }
    .back-button:hover {
      background-color: #0056b3;
    }
    .tx-details {
      margin-top: 20px;
    }
    .tx-details code a {
      color: #007BFF;
      text-decoration: none;
    }
    .tx-details code a:hover {
      text-decoration: underline;
    }
    .receipt-link {
      margin-top: 10px;
      display: block;
      font-size: 16px;
    }
  </style>
</head>
<body>
  <h1>Thank you for your donation!</h1>
  <div class="tx-details">
    <p>
      Transaction Hash:
      <code>
        <a href="{{.Explorer}}/tx/{{.TxHash}}" target="_blank" rel="noopener noreferrer">
          {{.TxHash}}
        </a>
      </code>
    </p>
    Status: <strong>{{.Status}}</strong><a class="receipt-link" href="{{.Facilitator}}?tx={{.TxHash}}&network={{.Network}}" target="_blank" rel="noopener noreferrer">
      📄 Check On-Chain Receipt
    </a>
  </div>

  <div class="story">{{.Content}}</div>

  <a href="." class="back-button">← Back to Landing Page</a>
</body>
</html>

